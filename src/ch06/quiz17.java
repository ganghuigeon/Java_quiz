package ch06;

public class quiz17 {
    /*
        다음 중 아래의 add메서드를 올바르게 오버로딩 한 것은? (모두 고르시오)

        long add(int a, int b) { return a+b;}

       ➊ long add(int x, int y) { return x+y;} int에서 int로 덮어쓰는 것으로 매개 변수 타입이 같기 때문에 오버로드 불가
       ➋ long add(long a, long b) { return a+b;} 가능
       ➌ int add(byte a, byte b) { return a+b;}  가능
       ➍ int add(long a, int b) { return (int)(a+b);} 가능
     */
}
